[project]
name = "aican-agent"
version = "0.2.0"
description = "AICAN voice agent — dual mode: Gemini native audio (free) or ElevenLabs pipeline"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "livekit-agents[google,groq,elevenlabs,silero,turn-detector]~=1.5",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv",
    "requests",
]

[tool.uv]
dev-dependencies = []
